% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_components.R
\name{download_components}
\alias{download_components}
\title{Download all mighty components from a repo location}
\usage{
download_components(repo, dest, overwrite = FALSE)
}
\arguments{
\item{repo}{\code{character(1)} location spec: \code{owner/repo},
\code{owner/repo/subdir}, or \code{owner/repo@ref}. When a subdir is given, only
that folder is searched.}

\item{dest}{\code{character(1)} directory to write component files into.
Created if it does not exist. Any existing \code{.R} and \code{.mustache} files are
removed first, so components deleted upstream do not linger. Other files
are left untouched.}

\item{overwrite}{\code{logical(1)} allow writing to a non-empty \code{dest}.
Errors if \code{FALSE} and \code{dest} is not empty.}
}
\value{
\code{dest}, invisibly.
}
\description{
Download every mighty component (\code{.R} and \code{.mustache} files) found in a
GitHub repo location and write them to a local directory.

The resulting directory can be passed to
\code{\link[mighty.component:list_components]{mighty.component::list_components()}}.
}
\details{
Components are searched for recursively below the given location, so both
a flat folder and the one-directory-per-component layout are supported:

\if{html}{\out{<div class="sourceCode">}}\preformatted{components/dummy/dummy.mustache
}\if{html}{\out{</div>}}

Files named \verb{test-*} are treated as component tests and are not
downloaded. Matching files are written flat into \code{dest}, discarding their
directory structure.
}
\examples{
dest <- withr::local_tempdir()
download_components(
  repo = "NovoNordisk-OpenSource/mighty.standards/components",
  dest = dest
)
mighty.component::list_components(dest)

}
